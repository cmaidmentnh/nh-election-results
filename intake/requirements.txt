# The intake service runs in its own virtualenv so upgrading the Anthropic SDK
# here can never disturb the Flask app or the Signal bots that share the box.
anthropic>=1.4.0
pydantic>=2.0
Flask>=3.0
Flask-Login>=0.6
# Phone photos: 12MP downscaling to the model's 2576px ceiling, and HEIC from
# iPhones, which the API does not accept directly.
Pillow>=10.0
pillow-heif>=0.16
# Gmail API: uses the mailbox's existing OAuth token instead of an app password.
google-api-python-client>=2.0
google-auth>=2.0
